# Source - https://stackoverflow.com/a/50761172
# Posted by Miq
# Retrieved 2026-08-31, License - CC BY-SA 4.0

services:
  backend:
    build: ../backend
    ports:
      - "8080:8080"
    networks:
      - project_network

networks:
  project_network:
    external: true
